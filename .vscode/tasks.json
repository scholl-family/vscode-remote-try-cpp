{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Main",
            "type": "shell",
            "command": "g++ -g -std=c++20 ${workspaceFolder}/*.cpp -o rooster",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}